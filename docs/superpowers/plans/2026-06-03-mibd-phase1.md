# MIBD Phase 1 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 为 Qwen3-VL-8B 和 InternVL3-8B 实现完整的 MIBD Phase 1 错误定位探测实验，提取各视觉条件下逐层隐状态，训练有害性方向探针，输出 Phase 1 JSON 摘要并生成 Go/No-Go 报告。

**架构：** 双 GPU 并行（GPU 0: Qwen3-VL，qwen3-vl env；GPU 1: InternVL3，rdo env）。数据集加载器将 HarmBench/MM-SafetyBench 文本转换为 MIBDSample；模型适配器封装 Qwen3-VL 和 InternVL3 的隐状态提取；提取 pipeline 遍历 layer×token_pos×visual_condition 实验矩阵，输出 npy 缓存；probe 训练用均值差方向 + AUC 评估；summary 模块聚合输出标准 JSON。

**技术栈：** Python 3.10, PyTorch 2.9(qwen3-vl env) / 2.5(rdo env), transformers 5.5.4 / 4.47.0, qwen-vl-utils, numpy, PyYAML

**执行环境：**
- Qwen3-VL-8B: `conda run -n qwen3-vl python -m experiments.mibd.run_phase1 --model qwen3vl --gpu 0`
- InternVL3-8B: `conda run -n rdo python -m experiments.mibd.run_phase1 --model internvl3 --gpu 1`

**关键路径：**
- Qwen3-VL-8B 模型: `/inspire/hdd/global_user/wenming-253108090054/models/Qwen3-VL-8B`
- InternVL3-8B 模型: `/inspire/hdd/global_user/wenming-253108090054/models/InternVL3-8B`
- HarmBench 多模态图片: `/inspire/hdd/global_user/wenming-253108090054/datasets/benchmarks/harmbench/data/multimodal_behavior_images/`
- MM-SafetyBench FigStep 图片: `/inspire/hdd/global_user/wenming-253108090054/czk/MML/dataset/mm-safebench/`
- 有害/无害文本数据: `data/saladbench_splits/`
- 工作目录: `/inspire/hdd/global_user/wenming-253108090054/zhujiaqi/geometry-of-refusal/`

---

## 新建/修改文件

| 文件 | 状态 | 职责 |
|------|------|------|
| `experiments/mibd/data/loaders.py` | 新建 | HarmBench+MM-SafetyBench+FigStep → MIBDSample |
| `experiments/mibd/data/image_utils.py` | 新建 | 生成 V-blank/V-noise 图片 |
| `experiments/mibd/models/adapters.py` | 新建 | Qwen3VLAdapter + InternVL3Adapter（隐状态提取） |
| `experiments/mibd/models/loader.py` | 新建 | load_model() 工厂函数 |
| `experiments/mibd/extraction/` | 新建目录 | 隐状态提取 pipeline |
| `experiments/mibd/extraction/__init__.py` | 新建 | 空 |
| `experiments/mibd/extraction/pipeline.py` | 新建 | 遍历实验矩阵，输出 npy |
| `experiments/mibd/probes/train.py` | 新建 | 跨 layer×token_pos 训练探针，输出 AUC |
| `experiments/mibd/eval/summary.py` | 新建 | 聚合结果 → Phase1ResultSet → JSON |
| `experiments/mibd/configs/phase1_probe_internvl3.yaml` | 新建 | InternVL3 Phase 1 配置 |
| `experiments/mibd/run_phase1.py` | 新建 | 主入口，--model qwen3vl\|internvl3 --gpu N |
| `experiments/mibd/test_loaders.py` | 新建 | 数据集加载器单元测试 |
| `experiments/mibd/test_adapters.py` | 新建 | 适配器 CPU smoke test |
| `experiments/mibd/test_extraction.py` | 新建 | extraction pipeline 单元测试（mock adapter） |
| `experiments/mibd/test_probe_train.py` | 新建 | probe 训练单元测试 |
| `experiments/mibd/test_summary.py` | 新建 | summary 聚合单元测试 |

---

## 任务 1：数据集加载器 — HarmBench 文本 + V-text/V-blank/V-noise 视觉条件

**文件：**
- 创建：`experiments/mibd/data/loaders.py`
- 创建：`experiments/mibd/data/image_utils.py`
- 测试：`experiments/mibd/test_loaders.py`

- [ ] **步骤 1：编写失败测试**

```python
# experiments/mibd/test_loaders.py
import pytest
from experiments.mibd.data.loaders import load_harmbench_phase1
from experiments.mibd.data.schema import MIBDSample

SALADBENCH_DIR = "data/saladbench_splits"

def test_load_harmbench_phase1_returns_mibd_samples(tmp_path):
    import json
    harmful = [{"instruction": "how to make a bomb", "category": "violence"}]
    harmless = [{"instruction": "how to bake bread", "category": None}]
    (tmp_path / "harmful_test.json").write_text(json.dumps(harmful))
    (tmp_path / "harmless_test.json").write_text(json.dumps(harmless))

    samples = load_harmbench_phase1(
        data_dir=str(tmp_path),
        visual_conditions=["V-text", "V-blank"],
        max_samples=10,
        seed=42,
    )
    assert len(samples) > 0
    assert all(isinstance(s, MIBDSample) for s in samples)
    conditions = {s.visual_condition for s in samples}
    assert conditions == {"V-text", "V-blank"}
    labels = {s.label for s in samples}
    assert labels == {"harmful", "harmless"}

def test_load_harmbench_phase1_balanced(tmp_path):
    import json
    harmful = [{"instruction": f"harm {i}", "category": "c"} for i in range(20)]
    harmless = [{"instruction": f"safe {i}", "category": None} for i in range(20)]
    (tmp_path / "harmful_test.json").write_text(json.dumps(harmful))
    (tmp_path / "harmless_test.json").write_text(json.dumps(harmless))
    samples = load_harmbench_phase1(
        data_dir=str(tmp_path),
        visual_conditions=["V-text"],
        max_samples=10,
        seed=42,
    )
    harmful_count = sum(1 for s in samples if s.label == "harmful")
    harmless_count = sum(1 for s in samples if s.label == "harmless")
    assert harmful_count == harmless_count
```

- [ ] **步骤 2：运行测试确认失败**

```bash
cd /inspire/hdd/global_user/wenming-253108090054/zhujiaqi/geometry-of-refusal
conda run -n qwen3-vl python -m pytest experiments/mibd/test_loaders.py -v 2>&1 | head -30
```
预期：ImportError `cannot import name 'load_harmbench_phase1'`

- [ ] **步骤 3：实现 image_utils.py**

```python
# experiments/mibd/data/image_utils.py
from __future__ import annotations
import numpy as np
from PIL import Image


def blank_image(size: tuple[int, int] = (336, 336)) -> Image.Image:
    return Image.fromarray(np.zeros((*size, 3), dtype=np.uint8))


def noise_image(size: tuple[int, int] = (336, 336), seed: int = 0) -> Image.Image:
    rng = np.random.default_rng(seed)
    arr = rng.integers(0, 256, (*size, 3), dtype=np.uint8)
    return Image.fromarray(arr)
```

- [ ] **步骤 4：实现 loaders.py**

```python
# experiments/mibd/data/loaders.py
from __future__ import annotations
import json
import random
import uuid
from pathlib import Path
from typing import Sequence

from experiments.mibd.data.schema import MIBDSample
from experiments.mibd.config import SUPPORTED_VISUAL_CONDITIONS


def load_harmbench_phase1(
    data_dir: str,
    visual_conditions: Sequence[str],
    max_samples: int = 512,
    seed: int = 42,
    split: str = "test",
) -> list[MIBDSample]:
    """Load HarmBench text samples and expand across visual conditions."""
    for vc in visual_conditions:
        if vc not in SUPPORTED_VISUAL_CONDITIONS:
            raise ValueError(f"Unsupported visual condition: {vc}")

    data_path = Path(data_dir)
    harmful_raw = json.loads((data_path / f"harmful_{split}.json").read_text())
    harmless_raw = json.loads((data_path / f"harmless_{split}.json").read_text())

    rng = random.Random(seed)
    n_per_label = max_samples // (2 * len(visual_conditions))
    n_per_label = max(1, n_per_label)

    harmful_sel = rng.sample(harmful_raw, min(n_per_label, len(harmful_raw)))
    harmless_sel = rng.sample(harmless_raw, min(n_per_label, len(harmless_raw)))

    samples: list[MIBDSample] = []
    for vc in visual_conditions:
        for item in harmful_sel:
            samples.append(MIBDSample.from_dict({
                "id": str(uuid.uuid4()),
                "text": item["instruction"],
                "image_path": None,
                "label": "harmful",
                "category": str(item.get("category") or "unknown"),
                "source": "harmbench",
                "paired_id": None,
                "visual_condition": vc,
            }))
        for item in harmless_sel:
            samples.append(MIBDSample.from_dict({
                "id": str(uuid.uuid4()),
                "text": item["instruction"],
                "image_path": None,
                "label": "harmless",
                "category": str(item.get("category") or "general"),
                "source": "alpaca",
                "paired_id": None,
                "visual_condition": vc,
            }))
    return samples
```

- [ ] **步骤 5：运行测试确认通过**

```bash
conda run -n qwen3-vl python -m pytest experiments/mibd/test_loaders.py -v
```
预期：2 PASSED

- [ ] **步骤 6：Commit**

```bash
git add experiments/mibd/data/loaders.py experiments/mibd/data/image_utils.py experiments/mibd/test_loaders.py
git commit -m "feat(mibd): add HarmBench loader and image utils"
```

---

## 任务 2：数据集加载器 — MM-SafetyBench FigStep 视觉条件

**文件：**
- 修改：`experiments/mibd/data/loaders.py`（新增 `load_mmsafety_figstep`）
- 修改：`experiments/mibd/test_loaders.py`

- [ ] **步骤 1：编写失败测试**

```python
# 追加到 experiments/mibd/test_loaders.py
from experiments.mibd.data.loaders import load_mmsafety_figstep

def test_load_mmsafety_figstep_returns_figstep_samples(tmp_path):
    import json
    cat_dir = tmp_path / "02"
    cat_dir.mkdir()
    figstep_dir = cat_dir / "images_figstep"
    figstep_dir.mkdir()
    img_file = figstep_dir / "1.png"
    from PIL import Image
    Image.new("RGB", (64, 64)).save(str(img_file))
    data = [{"id": 1, "original_prompt": "do something harmful", "qr_prompt": "do this"}]
    (cat_dir / "data.json").write_text(json.dumps(data))

    samples = load_mmsafety_figstep(
        mmsafety_dir=str(tmp_path),
        max_samples=10,
        seed=42,
    )
    assert len(samples) > 0
    assert all(s.visual_condition == "FigStep" for s in samples)
    assert all(s.image_path is not None for s in samples)
    assert all(s.label == "harmful" for s in samples)
```

- [ ] **步骤 2：运行确认失败**

```bash
conda run -n qwen3-vl python -m pytest experiments/mibd/test_loaders.py::test_load_mmsafety_figstep_returns_figstep_samples -v
```
预期：ImportError

- [ ] **步骤 3：实现 load_mmsafety_figstep**

在 `experiments/mibd/data/loaders.py` 末尾追加：

```python
def load_mmsafety_figstep(
    mmsafety_dir: str,
    max_samples: int = 512,
    seed: int = 42,
) -> list[MIBDSample]:
    """Load MM-SafetyBench FigStep images as harmful MIBDSamples."""
    base = Path(mmsafety_dir)
    all_items: list[dict] = []
    for cat_dir in sorted(base.iterdir()):
        if not cat_dir.is_dir():
            continue
        data_file = cat_dir / "data.json"
        figstep_dir = cat_dir / "images_figstep"
        if not data_file.exists() or not figstep_dir.exists():
            continue
        items = json.loads(data_file.read_text())
        for item in items:
            img_path = figstep_dir / f"{item['id']}.png"
            if img_path.exists():
                all_items.append({
                    "text": item.get("qr_prompt", item.get("original_prompt", "")),
                    "image_path": str(img_path),
                    "category": cat_dir.name,
                })

    rng = random.Random(seed)
    rng.shuffle(all_items)
    selected = all_items[:max_samples]

    return [
        MIBDSample.from_dict({
            "id": str(uuid.uuid4()),
            "text": it["text"],
            "image_path": it["image_path"],
            "label": "harmful",
            "category": it["category"],
            "source": "mm-safetybench",
            "paired_id": None,
            "visual_condition": "FigStep",
        })
        for it in selected
    ]
```

- [ ] **步骤 4：运行测试确认通过**

```bash
conda run -n qwen3-vl python -m pytest experiments/mibd/test_loaders.py -v
```
预期：3 PASSED

- [ ] **步骤 5：Commit**

```bash
git add experiments/mibd/data/loaders.py experiments/mibd/test_loaders.py
git commit -m "feat(mibd): add MM-SafetyBench FigStep loader"
```

---

## 任务 3：InternVL3 配置文件

**文件：**
- 创建：`experiments/mibd/configs/phase1_probe_internvl3.yaml`

- [ ] **步骤 1：创建 InternVL3 Phase 1 配置**

```yaml
# experiments/mibd/configs/phase1_probe_internvl3.yaml
model_id: /inspire/hdd/global_user/wenming-253108090054/models/InternVL3-8B
dataset: harmbench_phase1
visual_conditions:
  - V-text
  - V-blank
  - V-noise
  - V-real
  - FigStep
layer_grid:
  start: 0
  stop: 28
  step: 1
token_grid:
  - -8
  - -5
  - -3
  - -1
seed: 42
max_samples: 512
batch_size: 4
output_dir: results/mibd/phase1_probe/internvl3_8b
```

- [ ] **步骤 2：验证 config 可加载**

```bash
cd /inspire/hdd/global_user/wenming-253108090054/zhujiaqi/geometry-of-refusal
conda run -n rdo python -m experiments.mibd.inspect_config \
  --config experiments/mibd/configs/phase1_probe_internvl3.yaml
```
预期输出含 `experiment_count: 140`（28层 × 4位置 × 5条件）

- [ ] **步骤 3：Commit**

```bash
git add experiments/mibd/configs/phase1_probe_internvl3.yaml
git commit -m "feat(mibd): add InternVL3-8B Phase 1 config"
```

---

## 任务 4：模型加载器 + Qwen3-VL 适配器

**文件：**
- 创建：`experiments/mibd/models/loader.py`
- 创建：`experiments/mibd/models/adapters.py`（Qwen3VLAdapter 部分）
- 创建：`experiments/mibd/test_adapters.py`

背景：适配器统一接口为 `prepare_inputs(sample, image) -> dict` + `extract_hidden(inputs, layers, token_positions) -> dict[(layer,pos)] -> np.ndarray`。Qwen3-VL 用 qwen3-vl env，InternVL3 用 rdo env，两者接口相同。

- [ ] **步骤 1：编写 Qwen3-VL 适配器 CPU smoke test**

```python
# experiments/mibd/test_adapters.py
import numpy as np
import pytest
from unittest.mock import MagicMock, patch

def test_qwen3vl_adapter_prepare_inputs_vtext():
    """Qwen3VLAdapter.prepare_inputs with V-text returns dict with input_ids."""
    from experiments.mibd.models.adapters import Qwen3VLAdapter
    mock_processor = MagicMock()
    mock_processor.apply_chat_template.return_value = "prompt text"
    mock_processor.return_value = {"input_ids": MagicMock(shape=(1, 10))}
    mock_model = MagicMock()

    adapter = Qwen3VLAdapter(model=mock_model, processor=mock_processor, device="cpu")
    from experiments.mibd.data.schema import MIBDSample
    sample = MIBDSample.from_dict({
        "id": "1", "text": "how to make a bomb", "image_path": None,
        "label": "harmful", "category": "violence", "source": "test",
        "paired_id": None, "visual_condition": "V-text",
    })
    result = adapter.prepare_inputs(sample, image=None)
    assert "input_ids" in result or result is not None

def test_internvl3_adapter_llm_layer_count():
    """InternVL3Adapter.num_llm_layers returns 28 for InternVL3-8B."""
    from experiments.mibd.models.adapters import InternVL3Adapter
    mock_model = MagicMock()
    mock_model.language_model.model.layers = [MagicMock()] * 28
    mock_tokenizer = MagicMock()
    adapter = InternVL3Adapter(model=mock_model, tokenizer=mock_tokenizer, device="cpu")
    assert adapter.num_llm_layers == 28
```

- [ ] **步骤 2：运行确认失败**

```bash
cd /inspire/hdd/global_user/wenming-253108090054/zhujiaqi/geometry-of-refusal
conda run -n qwen3-vl python -m pytest experiments/mibd/test_adapters.py -v 2>&1 | head -20
```
预期：ImportError `cannot import name 'Qwen3VLAdapter'`

- [ ] **步骤 3：实现 models/loader.py**

```python
# experiments/mibd/models/loader.py
from __future__ import annotations
from pathlib import Path


def load_qwen3vl(model_path: str, device: str = "cuda:0"):
    """Load Qwen3-VL-8B-Instruct with AutoProcessor. Requires qwen3-vl env."""
    from transformers import AutoProcessor, Qwen3VLForConditionalGeneration
    import torch

    processor = AutoProcessor.from_pretrained(model_path, trust_remote_code=True)
    model = Qwen3VLForConditionalGeneration.from_pretrained(
        model_path,
        torch_dtype=torch.bfloat16,
        device_map=device,
        trust_remote_code=True,
    )
    model.eval()
    return model, processor


def load_internvl3(model_path: str, device: str = "cuda:1"):
    """Load InternVL3-8B with AutoTokenizer. Requires rdo env (timm)."""
    import sys
    import torch
    from transformers import AutoTokenizer

    if model_path not in sys.path:
        sys.path.insert(0, model_path)
    from modeling_internvl_chat import InternVLChatModel

    tokenizer = AutoTokenizer.from_pretrained(
        model_path, trust_remote_code=True, use_fast=False
    )
    model = InternVLChatModel.from_pretrained(
        model_path,
        torch_dtype=torch.bfloat16,
        device_map=device,
        trust_remote_code=True,
    )
    model.eval()
    return model, tokenizer
```

- [ ] **步骤 4：实现 models/adapters.py（Qwen3VLAdapter + InternVL3Adapter）**

```python
# experiments/mibd/models/adapters.py
from __future__ import annotations

from abc import ABC, abstractmethod
from typing import Any

import numpy as np
import torch
from PIL import Image

from experiments.mibd.data.schema import MIBDSample
from experiments.mibd.data.image_utils import blank_image, noise_image


class MIBDModelAdapter(ABC):
    """统一接口：输入准备 + 隐状态提取。"""

    @property
    @abstractmethod
    def num_llm_layers(self) -> int: ...

    @abstractmethod
    def prepare_inputs(
        self, sample: MIBDSample, image: Image.Image | None
    ) -> dict[str, Any]: ...

    @abstractmethod
    def extract_hidden(
        self,
        inputs: dict[str, Any],
        layers: tuple[int, ...],
        token_positions: tuple[int, ...],
    ) -> dict[tuple[int, int], np.ndarray]:
        """返回 {(layer, token_pos): hidden_vector shape (hidden_dim,)}"""
        ...

    def build_image_for_condition(
        self, sample: MIBDSample, seed: int = 0
    ) -> Image.Image | None:
        vc = sample.visual_condition
        if vc == "V-text":
            return None
        if vc == "V-blank":
            return blank_image()
        if vc == "V-noise":
            return noise_image(seed=seed)
        if vc in ("V-real", "FigStep"):
            if sample.image_path is None:
                return blank_image()
            return Image.open(sample.image_path).convert("RGB")
        return None


# ──────────────────────────────────────────────────────────────
# Qwen3-VL Adapter
# ──────────────────────────────────────────────────────────────
class Qwen3VLAdapter(MIBDModelAdapter):
    """
    Qwen3-VL-8B-Instruct 隐状态提取适配器。
    - V-text: 无图片，纯文本 prompt
    - V-blank/V-noise/V-real/FigStep: 带图片，mm prompt
    - 隐状态: model.model.layers[i] 输出, hook 方式提取
    - LLM layers: model.model.layers (32层)
    """

    def __init__(self, model, processor, device: str):
        self.model = model
        self.processor = processor
        self.device = device

    @property
    def num_llm_layers(self) -> int:
        return len(self.model.model.layers)

    def prepare_inputs(
        self, sample: MIBDSample, image: Image.Image | None
    ) -> dict[str, Any]:
        from qwen_vl_utils import process_vision_info

        if image is None:
            messages = [{"role": "user", "content": [
                {"type": "text", "text": sample.text}
            ]}]
            text = self.processor.apply_chat_template(
                messages, tokenize=False, add_generation_prompt=False
            )
            inputs = self.processor(text=[text], return_tensors="pt")
        else:
            messages = [{"role": "user", "content": [
                {"type": "image", "image": image},
                {"type": "text", "text": sample.text},
            ]}]
            text = self.processor.apply_chat_template(
                messages, tokenize=False, add_generation_prompt=False
            )
            image_inputs, _ = process_vision_info(messages)
            inputs = self.processor(
                text=[text], images=image_inputs, return_tensors="pt"
            )
        return {k: v.to(self.device) for k, v in inputs.items()}

    def extract_hidden(
        self,
        inputs: dict[str, Any],
        layers: tuple[int, ...],
        token_positions: tuple[int, ...],
    ) -> dict[tuple[int, int], np.ndarray]:
        layer_set = set(layers)
        captured: dict[int, torch.Tensor] = {}
        hooks = []

        for idx, layer in enumerate(self.model.model.layers):
            if idx not in layer_set:
                continue

            def make_hook(i):
                def hook(module, inp, out):
                    h = out[0] if isinstance(out, tuple) else out
                    captured[i] = h.detach().float().cpu()
                return hook

            hooks.append(layer.register_forward_hook(make_hook(idx)))

        with torch.no_grad():
            self.model(**inputs, output_hidden_states=False)

        for h in hooks:
            h.remove()

        result: dict[tuple[int, int], np.ndarray] = {}
        for layer_idx, hidden in captured.items():
            seq_len = hidden.shape[1]
            for pos in token_positions:
                abs_pos = seq_len + pos if pos < 0 else pos
                if 0 <= abs_pos < seq_len:
                    result[(layer_idx, pos)] = hidden[0, abs_pos, :].numpy()
        return result


# ──────────────────────────────────────────────────────────────
# InternVL3 Adapter
# ──────────────────────────────────────────────────────────────
class InternVL3Adapter(MIBDModelAdapter):
    """
    InternVL3-8B 隐状态提取适配器。
    - trust_remote_code=True, rdo env
    - 隐状态: model.language_model.model.layers[i], hook 方式提取
    - V-text: language_model(input_ids) 直接调用
    - 有图片: extract_feature + 替换 IMG_CONTEXT embeds → language_model(inputs_embeds)
    - LLM layers: model.language_model.model.layers (28层)
    """

    IMG_CONTEXT_TOKEN = "<IMG_CONTEXT>"
    INPUT_SIZE = 448

    def __init__(self, model, tokenizer, device: str):
        self.model = model
        self.tokenizer = tokenizer
        self.device = device
        self._img_context_token_id: int | None = None

    @property
    def num_llm_layers(self) -> int:
        return len(self.model.language_model.model.layers)

    def _get_img_context_token_id(self) -> int:
        if self._img_context_token_id is None:
            self._img_context_token_id = self.tokenizer.convert_tokens_to_ids(
                self.IMG_CONTEXT_TOKEN
            )
        return self._img_context_token_id

    def _preprocess_image(self, image: Image.Image) -> torch.Tensor:
        import torchvision.transforms as T
        from torchvision.transforms.functional import InterpolationMode

        transform = T.Compose([
            T.Lambda(lambda img: img.convert("RGB")),
            T.Resize((self.INPUT_SIZE, self.INPUT_SIZE),
                     interpolation=InterpolationMode.BICUBIC),
            T.ToTensor(),
            T.Normalize(mean=(0.485, 0.456, 0.406), std=(0.229, 0.224, 0.225)),
        ])
        return transform(image).unsqueeze(0).to(self.device).to(torch.bfloat16)

    def _build_text_prompt(self, text: str) -> str:
        import sys
        model_path = self.model.config._name_or_path if hasattr(
            self.model.config, "_name_or_path"
        ) else str(self.model.config.get("_name_or_path", ""))
        if model_path and model_path not in sys.path:
            sys.path.insert(0, model_path)
        from conversation import get_conv_template
        tmpl = get_conv_template(self.model.template)
        tmpl.system_message = self.model.system_message
        tmpl.append_message(tmpl.roles[0], text)
        tmpl.append_message(tmpl.roles[1], None)
        return tmpl.get_prompt()

    def _build_mm_prompt(self, text: str) -> str:
        import sys
        model_path = self.model.config._name_or_path if hasattr(
            self.model.config, "_name_or_path"
        ) else ""
        if model_path and model_path not in sys.path:
            sys.path.insert(0, model_path)
        from conversation import get_conv_template
        num_image_token = self.model.num_image_token
        img_tokens = (
            "<img>"
            + self.IMG_CONTEXT_TOKEN * num_image_token
            + "</img>"
        )
        question = f"{img_tokens}\n{text}"
        tmpl = get_conv_template(self.model.template)
        tmpl.system_message = self.model.system_message
        tmpl.append_message(tmpl.roles[0], question)
        tmpl.append_message(tmpl.roles[1], None)
        return tmpl.get_prompt()

    def prepare_inputs(
        self, sample: MIBDSample, image: Image.Image | None
    ) -> dict[str, Any]:
        if image is None:
            prompt = self._build_text_prompt(sample.text)
            input_ids = self.tokenizer(
                prompt, return_tensors="pt"
            ).input_ids.to(self.device)
            return {"input_ids": input_ids, "_pixel_values": None}
        else:
            prompt = self._build_mm_prompt(sample.text)
            input_ids = self.tokenizer(
                prompt, return_tensors="pt"
            ).input_ids.to(self.device)
            pixel_values = self._preprocess_image(image)
            return {"input_ids": input_ids, "_pixel_values": pixel_values}

    def _build_inputs_embeds(
        self,
        input_ids: torch.Tensor,
        pixel_values: torch.Tensor | None,
    ) -> torch.Tensor:
        embed_fn = self.model.language_model.get_input_embeddings()
        inputs_embeds = embed_fn(input_ids)
        if pixel_values is None:
            return inputs_embeds
        vit_embeds = self.model.extract_feature(pixel_values)
        ctx_id = self._get_img_context_token_id()
        B, N, C = inputs_embeds.shape
        flat_embeds = inputs_embeds.reshape(B * N, C)
        flat_ids = input_ids.reshape(B * N)
        mask = flat_ids == ctx_id
        flat_embeds[mask] = vit_embeds.reshape(-1, C).to(flat_embeds.device)
        return flat_embeds.reshape(B, N, C)

    def extract_hidden(
        self,
        inputs: dict[str, Any],
        layers: tuple[int, ...],
        token_positions: tuple[int, ...],
    ) -> dict[tuple[int, int], np.ndarray]:
        input_ids = inputs["input_ids"]
        pixel_values = inputs.get("_pixel_values")
        inputs_embeds = self._build_inputs_embeds(input_ids, pixel_values)

        layer_set = set(layers)
        captured: dict[int, torch.Tensor] = {}
        hooks = []

        for idx, layer in enumerate(self.model.language_model.model.layers):
            if idx not in layer_set:
                continue

            def make_hook(i):
                def hook(module, inp, out):
                    h = out[0] if isinstance(out, tuple) else out
                    captured[i] = h.detach().float().cpu()
                return hook

            hooks.append(layer.register_forward_hook(make_hook(idx)))

        with torch.no_grad():
            self.model.language_model(
                inputs_embeds=inputs_embeds,
                output_hidden_states=False,
            )

        for h in hooks:
            h.remove()

        result: dict[tuple[int, int], np.ndarray] = {}
        for layer_idx, hidden in captured.items():
            seq_len = hidden.shape[1]
            for pos in token_positions:
                abs_pos = seq_len + pos if pos < 0 else pos
                if 0 <= abs_pos < seq_len:
                    result[(layer_idx, pos)] = hidden[0, abs_pos, :].numpy()
        return result
```

- [ ] **步骤 5：运行 adapter smoke test**

```bash
conda run -n qwen3-vl python -m pytest experiments/mibd/test_adapters.py -v
```
预期：2 PASSED

- [ ] **步骤 6：Commit**

```bash
git add experiments/mibd/models/loader.py experiments/mibd/models/adapters.py \
  experiments/mibd/test_adapters.py
git commit -m "feat(mibd): add Qwen3VL and InternVL3 adapters with hidden state extraction"
```

---

## 任务 5：隐状态提取 Pipeline

**文件：**
- 创建：`experiments/mibd/extraction/__init__.py`
- 创建：`experiments/mibd/extraction/pipeline.py`
- 创建：`experiments/mibd/test_extraction.py`

- [ ] **步骤 1：编写 pipeline 测试（mock adapter）**

```python
# experiments/mibd/test_extraction.py
import numpy as np
import pytest
from unittest.mock import MagicMock

from experiments.mibd.data.schema import MIBDSample
from experiments.mibd.extraction.pipeline import run_extraction


def _make_sample(vc: str, label: str) -> MIBDSample:
    return MIBDSample.from_dict({
        "id": f"{vc}_{label}", "text": "test text", "image_path": None,
        "label": label, "category": "test", "source": "test",
        "paired_id": None, "visual_condition": vc,
    })


def test_run_extraction_returns_correct_keys():
    mock_adapter = MagicMock()
    mock_adapter.build_image_for_condition.return_value = None
    mock_adapter.prepare_inputs.return_value = {"input_ids": MagicMock()}
    mock_adapter.extract_hidden.return_value = {
        (0, -1): np.ones(16), (0, -5): np.ones(16),
        (1, -1): np.ones(16) * 2,
    }

    samples = [_make_sample("V-text", "harmful"), _make_sample("V-text", "harmless")]
    layers = (0, 1)
    token_positions = (-1, -5)

    result = run_extraction(mock_adapter, samples, layers, token_positions)

    assert "V-text" in result
    assert (0, -1) in result["V-text"]
    assert "harmful" in result["V-text"][(0, -1)]
    assert "harmless" in result["V-text"][(0, -1)]
    arr = result["V-text"][(0, -1)]["harmful"]
    assert arr.ndim == 2  # (n_samples, hidden_dim)


def test_run_extraction_skips_missing_hidden():
    mock_adapter = MagicMock()
    mock_adapter.build_image_for_condition.return_value = None
    mock_adapter.prepare_inputs.return_value = {"input_ids": MagicMock()}
    # 只返回 (0, -1)，缺少 (1, -5)
    mock_adapter.extract_hidden.return_value = {(0, -1): np.ones(8)}

    samples = [_make_sample("V-blank", "harmful")]
    result = run_extraction(mock_adapter, samples, layers=(0, 1), token_positions=(-1, -5))

    assert (0, -1) in result["V-blank"]
    assert (1, -5) not in result.get("V-blank", {})
```

- [ ] **步骤 2：运行确认失败**

```bash
conda run -n qwen3-vl python -m pytest experiments/mibd/test_extraction.py -v 2>&1 | head -15
```
预期：ImportError

- [ ] **步骤 3：实现 extraction/pipeline.py**

```python
# experiments/mibd/extraction/pipeline.py
from __future__ import annotations

from collections import defaultdict
from typing import Sequence

import numpy as np

from experiments.mibd.data.schema import MIBDSample
from experiments.mibd.models.adapters import MIBDModelAdapter


def run_extraction(
    adapter: MIBDModelAdapter,
    samples: Sequence[MIBDSample],
    layers: tuple[int, ...],
    token_positions: tuple[int, ...],
    seed: int = 0,
) -> dict[str, dict[tuple[int, int], dict[str, list[np.ndarray]]]]:
    """
    遍历样本，提取隐状态。
    返回: {visual_condition: {(layer, pos): {"harmful": [vec, ...], "harmless": [vec, ...]}}}
    """
    # structure: vc -> (layer, pos) -> label -> list of vectors
    storage: dict[str, dict[tuple[int, int], dict[str, list[np.ndarray]]]] = \
        defaultdict(lambda: defaultdict(lambda: defaultdict(list)))

    for i, sample in enumerate(samples):
        image = adapter.build_image_for_condition(sample, seed=seed + i)
        inputs = adapter.prepare_inputs(sample, image)
        hidden_map = adapter.extract_hidden(inputs, layers, token_positions)

        for (layer_idx, pos), vec in hidden_map.items():
            storage[sample.visual_condition][(layer_idx, pos)][sample.label].append(vec)

    # convert lists to 2D arrays
    result: dict[str, dict[tuple[int, int], dict[str, np.ndarray]]] = {}
    for vc, lp_map in storage.items():
        result[vc] = {}
        for (l, p), label_map in lp_map.items():
            result[vc][(l, p)] = {
                label: np.stack(vecs, axis=0)
                for label, vecs in label_map.items()
                if len(vecs) > 0
            }
    return result
```

- [ ] **步骤 4：创建 extraction/__init__.py**

```python
# experiments/mibd/extraction/__init__.py
```

- [ ] **步骤 5：运行测试确认通过**

```bash
conda run -n qwen3-vl python -m pytest experiments/mibd/test_extraction.py -v
```
预期：2 PASSED

- [ ] **步骤 6：Commit**

```bash
git add experiments/mibd/extraction/__init__.py experiments/mibd/extraction/pipeline.py \
  experiments/mibd/test_extraction.py
git commit -m "feat(mibd): add hidden state extraction pipeline"
```

---

## 任务 6：Probe 训练（均值差方向 + AUC）

**文件：**
- 创建：`experiments/mibd/probes/train.py`
- 创建：`experiments/mibd/test_probe_train.py`

已有基础工具：`probes/direction.py`（mean_difference_direction, project_scores, cosine_similarity）和 `probes/metrics.py`（binary_auc）。本任务在其上构建跨 layer×condition 的训练循环。

- [ ] **步骤 1：编写 probe 训练测试**

```python
# experiments/mibd/test_probe_train.py
import numpy as np
import pytest

from experiments.mibd.probes.train import (
    train_probes_for_condition,
    compute_condition_cosines,
    compute_static_transfer_aucs,
)


def _make_hidden_map(n: int = 20, dim: int = 32):
    """返回一个 (layer,pos)->{"harmful":arr,"harmless":arr} 字典，harmful 均值高。"""
    rng = np.random.default_rng(0)
    result = {}
    for layer in (0, 1):
        for pos in (-1, -5):
            harmful = rng.standard_normal((n, dim)) + 2.0
            harmless = rng.standard_normal((n, dim))
            result[(layer, pos)] = {"harmful": harmful, "harmless": harmless}
    return result


def test_train_probes_returns_auc_above_chance():
    hidden_map = _make_hidden_map()
    probe_results = train_probes_for_condition(hidden_map)
    for (layer, pos), info in probe_results.items():
        assert "auc" in info
        assert "direction" in info
        assert 0.0 <= info["auc"] <= 1.0
        assert info["auc"] > 0.5  # separable data 应高于随机


def test_compute_condition_cosines_symmetric_keys():
    cond_directions = {
        "V-text": np.array([1.0, 0.0, 0.0]),
        "V-blank": np.array([0.9, 0.1, 0.0]),
        "V-noise": np.array([0.85, 0.15, 0.0]),
    }
    cosines = compute_condition_cosines(cond_directions)
    assert ("V-text", "V-blank") in cosines or ("V-blank", "V-text") in cosines
    for val in cosines.values():
        assert -1.0 <= val <= 1.0


def test_compute_static_transfer_aucs():
    # direction from V-text; apply to V-blank hidden states
    rng = np.random.default_rng(1)
    dim = 16
    direction = np.ones(dim) / np.sqrt(dim)
    target_hidden = {
        "V-blank": {
            (0, -1): {
                "harmful": rng.standard_normal((10, dim)) + 2.0,
                "harmless": rng.standard_normal((10, dim)),
            }
        }
    }
    aucs = compute_static_transfer_aucs(
        source_direction=direction,
        source_condition="V-text",
        target_hidden_maps=target_hidden,
        layer=0,
        pos=-1,
    )
    assert ("V-text", "V-blank") in aucs
    assert aucs[("V-text", "V-blank")] > 0.5
```

- [ ] **步骤 2：运行确认失败**

```bash
conda run -n qwen3-vl python -m pytest experiments/mibd/test_probe_train.py -v 2>&1 | head -15
```

- [ ] **步骤 3：实现 probes/train.py**

```python
# experiments/mibd/probes/train.py
from __future__ import annotations

from typing import Sequence
import numpy as np

from experiments.mibd.probes.direction import (
    mean_difference_direction,
    project_scores,
    cosine_similarity,
)
from experiments.mibd.probes.metrics import binary_auc


def train_probes_for_condition(
    hidden_map: dict[tuple[int, int], dict[str, np.ndarray]],
) -> dict[tuple[int, int], dict]:
    """
    对单个视觉条件的所有 (layer, pos) 训练均值差探针。
    返回: {(layer, pos): {"direction": ndarray, "auc": float}}
    """
    results = {}
    for (layer, pos), label_map in hidden_map.items():
        harmful = label_map.get("harmful")
        harmless = label_map.get("harmless")
        if harmful is None or harmless is None:
            continue
        if len(harmful) < 2 or len(harmless) < 2:
            continue
        direction = mean_difference_direction(harmful, harmless)
        all_hidden = np.vstack([harmful, harmless])
        labels = np.array([1] * len(harmful) + [0] * len(harmless))
        scores = project_scores(all_hidden, direction)
        auc = binary_auc(labels, scores)
        results[(layer, pos)] = {"direction": direction, "auc": auc}
    return results


def find_best_locus(
    probe_results: dict[tuple[int, int], dict],
) -> tuple[int, int]:
    """返回 AUC 最高的 (layer, pos)。"""
    return max(probe_results, key=lambda k: probe_results[k]["auc"])


def compute_condition_cosines(
    condition_directions: dict[str, np.ndarray],
) -> dict[tuple[str, str], float]:
    """
    计算所有条件对之间的方向余弦相似度。
    返回: {(cond_a, cond_b): cosine}
    """
    conditions = sorted(condition_directions.keys())
    cosines = {}
    for i, ca in enumerate(conditions):
        for cb in conditions[i + 1:]:
            cosines[(ca, cb)] = cosine_similarity(
                condition_directions[ca], condition_directions[cb]
            )
    return cosines


def compute_static_transfer_aucs(
    source_direction: np.ndarray,
    source_condition: str,
    target_hidden_maps: dict[str, dict[tuple[int, int], dict[str, np.ndarray]]],
    layer: int,
    pos: int,
) -> dict[tuple[str, str], float]:
    """
    用 source_condition 训练的方向在 target 条件上做 AUC 评估。
    返回: {(source_condition, target_condition): auc}
    """
    aucs = {}
    for target_cond, hidden_map in target_hidden_maps.items():
        if target_cond == source_condition:
            continue
        lp_map = hidden_map.get((layer, pos))
        if lp_map is None:
            continue
        harmful = lp_map.get("harmful")
        harmless = lp_map.get("harmless")
        if harmful is None or harmless is None:
            continue
        all_hidden = np.vstack([harmful, harmless])
        labels = np.array([1] * len(harmful) + [0] * len(harmless))
        scores = project_scores(all_hidden, source_direction)
        aucs[(source_condition, target_cond)] = binary_auc(labels, scores)
    return aucs
```

- [ ] **步骤 4：运行测试确认通过**

```bash
conda run -n qwen3-vl python -m pytest experiments/mibd/test_probe_train.py -v
```
预期：3 PASSED

- [ ] **步骤 5：Commit**

```bash
git add experiments/mibd/probes/train.py experiments/mibd/test_probe_train.py
git commit -m "feat(mibd): add probe training with direction + AUC across layer/pos"
```

---

## 任务 7：Summary 聚合 → Phase 1 JSON

**文件：**
- 创建：`experiments/mibd/eval/summary.py`
- 创建：`experiments/mibd/test_summary.py`

- [ ] **步骤 1：编写 summary 测试**

```python
# experiments/mibd/test_summary.py
import json
import numpy as np
import pytest

from experiments.mibd.eval.summary import build_phase1_summary


def _make_probe_results(auc: float):
    return {(17, -5): {"direction": np.ones(32) / np.sqrt(32), "auc": auc}}


def test_build_phase1_summary_schema():
    probe_results_by_condition = {
        "V-text":  _make_probe_results(0.91),
        "V-blank": _make_probe_results(0.72),
        "V-noise": _make_probe_results(0.70),
    }
    condition_cosines = {("V-text", "V-blank"): 0.52, ("V-blank", "V-noise"): 0.93}
    static_transfer = {("V-text", "V-blank"): 0.71}

    summary = build_phase1_summary(
        model_id="Qwen/Qwen3-VL-8B-Instruct",
        signal_type="harmfulness",
        probe_results_by_condition=probe_results_by_condition,
        condition_cosines=condition_cosines,
        static_transfer_auc=static_transfer,
    )
    # validate JSON serialisable
    raw = json.loads(json.dumps(summary))
    assert raw["model_id"] == "Qwen/Qwen3-VL-8B-Instruct"
    assert raw["signal_type"] == "harmfulness"
    assert len(raw["results"]) == 3
    for r in raw["results"]:
        assert "visual_condition" in r
        assert "layer" in r
        assert "token_pos" in r
        assert "auc" in r
    assert "V-text|V-blank" in raw["condition_cosines"]
    assert "V-text|V-blank" in raw["static_transfer_auc"]
```

- [ ] **步骤 2：运行确认失败**

```bash
conda run -n qwen3-vl python -m pytest experiments/mibd/test_summary.py -v 2>&1 | head -10
```

- [ ] **步骤 3：实现 eval/summary.py**

```python
# experiments/mibd/eval/summary.py
from __future__ import annotations
import json
from pathlib import Path

import numpy as np

from experiments.mibd.probes.train import find_best_locus


def build_phase1_summary(
    model_id: str,
    signal_type: str,
    probe_results_by_condition: dict[str, dict[tuple[int, int], dict]],
    condition_cosines: dict[tuple[str, str], float],
    static_transfer_auc: dict[tuple[str, str], float],
) -> dict:
    """
    聚合各视觉条件的 probe 结果，构造符合 HANDOFF.md 的 JSON 摘要。
    每个条件取 best-locus（最高 AUC）的 layer/pos/auc。
    """
    results = []
    for vc, pr in probe_results_by_condition.items():
        if not pr:
            continue
        best = find_best_locus(pr)
        layer, pos = best
        results.append({
            "visual_condition": vc,
            "layer": layer,
            "token_pos": pos,
            "auc": float(pr[best]["auc"]),
        })

    return {
        "model_id": model_id,
        "signal_type": signal_type,
        "results": results,
        "condition_cosines": {
            f"{a}|{b}": float(v)
            for (a, b), v in condition_cosines.items()
        },
        "static_transfer_auc": {
            f"{a}|{b}": float(v)
            for (a, b), v in static_transfer_auc.items()
        },
    }


def save_summary(summary: dict, output_dir: str, filename: str = "summary.json") -> Path:
    out = Path(output_dir)
    out.mkdir(parents=True, exist_ok=True)
    path = out / filename
    path.write_text(json.dumps(summary, indent=2, ensure_ascii=False))
    return path
```

- [ ] **步骤 4：运行测试确认通过**

```bash
conda run -n qwen3-vl python -m pytest experiments/mibd/test_summary.py -v
```
预期：1 PASSED

- [ ] **步骤 5：Commit**

```bash
git add experiments/mibd/eval/summary.py experiments/mibd/test_summary.py
git commit -m "feat(mibd): add Phase 1 summary builder"
```

---

## 任务 8：主入口 run_phase1.py + 双卡并行启动脚本

**文件：**
- 创建：`experiments/mibd/run_phase1.py`
- 创建：`scripts/run_mibd_phase1.sh`

- [ ] **步骤 1：实现 run_phase1.py**

```python
# experiments/mibd/run_phase1.py
"""
Phase 1 实验主入口。

用法:
  # Qwen3-VL (qwen3-vl env, GPU 0)
  conda run -n qwen3-vl python -m experiments.mibd.run_phase1 \
    --model qwen3vl --gpu 0 \
    --config experiments/mibd/configs/phase1_probe.yaml \
    --data-dir data/saladbench_splits \
    --mmsafety-dir /inspire/hdd/global_user/wenming-253108090054/czk/MML/dataset/mm-safebench

  # InternVL3 (rdo env, GPU 1)
  conda run -n rdo python -m experiments.mibd.run_phase1 \
    --model internvl3 --gpu 1 \
    --config experiments/mibd/configs/phase1_probe_internvl3.yaml \
    --data-dir data/saladbench_splits \
    --mmsafety-dir /inspire/hdd/global_user/wenming-253108090054/czk/MML/dataset/mm-safebench
"""
from __future__ import annotations

import argparse
import json
import os

from experiments.mibd.config import load_experiment_config
from experiments.mibd.data.loaders import load_harmbench_phase1, load_mmsafety_figstep
from experiments.mibd.eval.phase1_report import build_go_no_go_report, Phase1ResultSet, LocusResult
from experiments.mibd.eval.summary import build_phase1_summary, save_summary
from experiments.mibd.extraction.pipeline import run_extraction
from experiments.mibd.probes.train import (
    train_probes_for_condition,
    compute_condition_cosines,
    compute_static_transfer_aucs,
    find_best_locus,
)


def main():
    parser = argparse.ArgumentParser()
    parser.add_argument("--model", required=True, choices=["qwen3vl", "internvl3"])
    parser.add_argument("--gpu", type=int, default=0)
    parser.add_argument("--config", required=True)
    parser.add_argument("--data-dir", default="data/saladbench_splits")
    parser.add_argument("--mmsafety-dir",
        default="/inspire/hdd/global_user/wenming-253108090054/czk/MML/dataset/mm-safebench")
    args = parser.parse_args()

    os.environ["CUDA_VISIBLE_DEVICES"] = str(args.gpu)
    device = "cuda:0"  # after CUDA_VISIBLE_DEVICES remapping

    cfg = load_experiment_config(args.config)
    print(f"[run_phase1] model={args.model} gpu={args.gpu} "
          f"conditions={cfg.visual_conditions} layers={len(cfg.layers)} "
          f"max_samples={cfg.max_samples}")

    # 1. Load model
    if args.model == "qwen3vl":
        from experiments.mibd.models.loader import load_qwen3vl
        from experiments.mibd.models.adapters import Qwen3VLAdapter
        model, processor = load_qwen3vl(cfg.model_id, device=device)
        adapter = Qwen3VLAdapter(model=model, processor=processor, device=device)
    else:
        from experiments.mibd.models.loader import load_internvl3
        from experiments.mibd.models.adapters import InternVL3Adapter
        model, tokenizer = load_internvl3(cfg.model_id, device=device)
        adapter = InternVL3Adapter(model=model, tokenizer=tokenizer, device=device)

    # 2. Load samples
    text_conditions = [vc for vc in cfg.visual_conditions if vc != "FigStep"]
    figstep_in_conditions = "FigStep" in cfg.visual_conditions

    text_samples = load_harmbench_phase1(
        data_dir=args.data_dir,
        visual_conditions=text_conditions,
        max_samples=cfg.max_samples,
        seed=cfg.seed,
    )
    figstep_samples = []
    if figstep_in_conditions:
        figstep_samples = load_mmsafety_figstep(
            mmsafety_dir=args.mmsafety_dir,
            max_samples=cfg.max_samples // len(cfg.visual_conditions),
            seed=cfg.seed,
        )
    all_samples = text_samples + figstep_samples
    print(f"[run_phase1] loaded {len(all_samples)} samples")

    # 3. Extract hidden states
    all_hidden = run_extraction(
        adapter=adapter,
        samples=all_samples,
        layers=cfg.layers,
        token_positions=cfg.token_positions,
        seed=cfg.seed,
    )

    # 4. Train probes per condition
    probe_results_by_condition = {
        vc: train_probes_for_condition(all_hidden[vc])
        for vc in all_hidden
    }

    # 5. Compute condition cosines (at best V-text locus)
    vtext_probes = probe_results_by_condition.get("V-text", {})
    if vtext_probes:
        best_layer, best_pos = find_best_locus(vtext_probes)
        condition_directions = {
            vc: probe_results_by_condition[vc][(best_layer, best_pos)]["direction"]
            for vc in probe_results_by_condition
            if (best_layer, best_pos) in probe_results_by_condition[vc]
        }
        condition_cosines = compute_condition_cosines(condition_directions)
        static_transfer = compute_static_transfer_aucs(
            source_direction=vtext_probes[(best_layer, best_pos)]["direction"],
            source_condition="V-text",
            target_hidden_maps=all_hidden,
            layer=best_layer,
            pos=best_pos,
        )
    else:
        condition_cosines = {}
        static_transfer = {}

    # 6. Build and save summary
    summary = build_phase1_summary(
        model_id=cfg.model_id,
        signal_type="harmfulness",
        probe_results_by_condition=probe_results_by_condition,
        condition_cosines=condition_cosines,
        static_transfer_auc=static_transfer,
    )
    summary_path = save_summary(summary, output_dir=str(cfg.output_dir))
    print(f"[run_phase1] summary saved to {summary_path}")

    # 7. Build Go/No-Go report
    result_set = Phase1ResultSet(
        model_id=summary["model_id"],
        signal_type=summary["signal_type"],
        results=[
            LocusResult(
                visual_condition=r["visual_condition"],
                layer=r["layer"],
                token_pos=r["token_pos"],
                auc=r["auc"],
            )
            for r in summary["results"]
        ],
        condition_cosines={
            tuple(k.split("|", 1)): v
            for k, v in summary["condition_cosines"].items()
        },
        static_transfer_auc={
            tuple(k.split("|", 1)): v
            for k, v in summary["static_transfer_auc"].items()
        },
    )
    report = build_go_no_go_report(result_set)
    report_path = cfg.output_dir / "phase1_report.md"
    report_path.parent.mkdir(parents=True, exist_ok=True)
    report_path.write_text(report.markdown)
    print(f"[run_phase1] decision={report.decision}")
    print(f"[run_phase1] report saved to {report_path}")


if __name__ == "__main__":
    main()
```

- [ ] **步骤 2：创建双卡并行启动脚本**

```bash
# scripts/run_mibd_phase1.sh
#!/bin/bash
set -e

ROOT="/inspire/hdd/global_user/wenming-253108090054/zhujiaqi/geometry-of-refusal"
MMSAFETY="/inspire/hdd/global_user/wenming-253108090054/czk/MML/dataset/mm-safebench"

cd "$ROOT"

echo "Starting Qwen3-VL on GPU 0..."
conda run -n qwen3-vl python -m experiments.mibd.run_phase1 \
  --model qwen3vl --gpu 0 \
  --config experiments/mibd/configs/phase1_probe.yaml \
  --data-dir data/saladbench_splits \
  --mmsafety-dir "$MMSAFETY" \
  > logs/mibd_phase1_qwen3vl.log 2>&1 &
PID_QWEN=$!

echo "Starting InternVL3 on GPU 1..."
conda run -n rdo python -m experiments.mibd.run_phase1 \
  --model internvl3 --gpu 1 \
  --config experiments/mibd/configs/phase1_probe_internvl3.yaml \
  --data-dir data/saladbench_splits \
  --mmsafety-dir "$MMSAFETY" \
  > logs/mibd_phase1_internvl3.log 2>&1 &
PID_INTERN=$!

echo "Qwen3-VL PID=$PID_QWEN, InternVL3 PID=$PID_INTERN"
echo "Logs: logs/mibd_phase1_qwen3vl.log and logs/mibd_phase1_internvl3.log"
wait $PID_QWEN && echo "Qwen3-VL DONE" || echo "Qwen3-VL FAILED"
wait $PID_INTERN && echo "InternVL3 DONE" || echo "InternVL3 FAILED"
```

- [ ] **步骤 3：创建 logs 目录并运行 smoke test（CPU 验证 import）**

```bash
mkdir -p /inspire/hdd/global_user/wenming-253108090054/zhujiaqi/geometry-of-refusal/logs

# 验证主入口可以 import（不实际加载模型）
cd /inspire/hdd/global_user/wenming-253108090054/zhujiaqi/geometry-of-refusal
conda run -n qwen3-vl python -c "
import sys; sys.argv = ['run_phase1', '--help']
from experiments.mibd.run_phase1 import main
print('import OK')
" 2>&1 | head -5
```
预期：打印 `import OK` 或 help 文本（无报错）

- [ ] **步骤 4：运行全部单元测试**

```bash
cd /inspire/hdd/global_user/wenming-253108090054/zhujiaqi/geometry-of-refusal
conda run -n qwen3-vl python -m pytest \
  experiments/mibd/test_loaders.py \
  experiments/mibd/test_adapters.py \
  experiments/mibd/test_extraction.py \
  experiments/mibd/test_probe_train.py \
  experiments/mibd/test_summary.py \
  -v
```
预期：全部 PASSED（约 9-10 个测试）

- [ ] **步骤 5：Commit**

```bash
chmod +x scripts/run_mibd_phase1.sh
git add experiments/mibd/run_phase1.py scripts/run_mibd_phase1.sh
git commit -m "feat(mibd): add Phase 1 main entry point and dual-GPU launch script"
```

---

## 任务 9：启动实验

- [ ] **步骤 1：确认 GPU 状态**

```bash
nvidia-smi
```

- [ ] **步骤 2：启动双卡并行实验**

```bash
cd /inspire/hdd/global_user/wenming-253108090054/zhujiaqi/geometry-of-refusal
bash scripts/run_mibd_phase1.sh
```

- [ ] **步骤 3：监控日志**

```bash
# 两个窗口分别监控
tail -f logs/mibd_phase1_qwen3vl.log
tail -f logs/mibd_phase1_internvl3.log
```

- [ ] **步骤 4：实验完成后生成报告**

结果文件在：
- `results/mibd/phase1_probe/qwen3_vl_8b/summary.json`
- `results/mibd/phase1_probe/qwen3_vl_8b/phase1_report.md`
- `results/mibd/phase1_probe/internvl3_8b/summary.json`
- `results/mibd/phase1_probe/internvl3_8b/phase1_report.md`

如果两个模型的决策都是 `CONTINUE_MIBD`，继续 Phase 2（router + MIBD 训练）。

---

## 自检

**规格覆盖度：**
- ✅ HarmBench 数据集加载器 → 任务 1
- ✅ MM-SafetyBench FigStep 加载器 → 任务 2
- ✅ V-blank/V-noise 程序生成 → 任务 1（image_utils）
- ✅ V-real（harmbench multimodal images）→ loaders.py image_path 字段 + adapter.build_image_for_condition
- ✅ Qwen3-VL 适配器（输入准备 + 隐状态提取）→ 任务 4
- ✅ InternVL3 适配器 → 任务 4
- ✅ token audit → 已有 models/token_audit.py（HANDOFF.md 已标注，可按需集成）
- ✅ Probe 训练（均值差方向 + AUC）→ 任务 6
- ✅ Phase 1 JSON 摘要 → 任务 7
- ✅ Go/No-Go 报告 → 任务 8（使用已有 eval/phase1_report.py）
- ✅ 双卡并行启动 → 任务 9
- ✅ InternVL2-8B 不需要（实验方案只用 Qwen3-VL 和 InternVL3）

**注意：** `run_phase1.py` 中 `Phase1ResultSet.condition_cosines` 和 `static_transfer_auc` 的键为 `tuple[str, str]`，从 JSON 的 `"A|B"` 格式解析时已做 `tuple(k.split("|", 1))` 转换，与 `eval/phase1_report.py:56-64` 的查找逻辑一致。

