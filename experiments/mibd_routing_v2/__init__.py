"""MIBD routing v2: iteration directory for the refusal-routing-failure upgrade.

This package intentionally lives next to ``experiments.mibd_routing`` so that the
v2 iteration is fully isolated and can be reverted by deleting this directory.
Only CPU-runnable, GPU-free modules live here for now.
"""
